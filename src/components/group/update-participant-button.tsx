import React from "react";
import { Button } from "../ui/button";

type Props = {};

const UpdateParticipantButton = (props: Props) => {
  return <Button>Update group participants</Button>;
};

export default UpdateParticipantButton;
